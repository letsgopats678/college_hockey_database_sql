CREATE TABLE Prospects (  
    id INT PRIMARY KEY,
    firstname VARCHAR(30) NOT NULL,
    lastname VARCHAR(30) NOT NULL,
    college VARCHAR(30) NOT NULL,
    nhlteam VARCHAR(30) NOT NULL,
    age INT NOT NULL,
    position VARCHAR(30) NOT NULL,
    draftselection VARCHAR(30)
);
